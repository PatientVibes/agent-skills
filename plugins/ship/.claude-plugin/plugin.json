{
  "name": "ship",
  "description": "Subagent driving an approved change from branch → PR → external review → merge in one autonomous flow. Codex primary; falls back to dispatching the `pr-review` subagent (which runs the agent-tool-pr-reviewer CLI in multi-model consensus mode). No opencode/OpenRouter dependency in the fallback path.",
  "author": {
    "name": "Chris Moore",
    "email": "chris.paul.moore@gmail.com"
  }
}
